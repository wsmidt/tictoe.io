#route for LB healthchecks
GET     /healthcheck                controllers.HealthCheckController.healthCheck

#establishes a websocket for joining and playing a game meow!
GET     /connect                    controllers.TicToeController.connect
